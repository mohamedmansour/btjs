{
  "name": "@internal/fast-btr-tools",
  "version": "1.0.0",
  "description": "Internal tooling for BTR for build / run",
  "main": "./dist/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild ./src/*.ts --outdir=./dist --sourcemap --format=esm --target=esnext --platform=node"
  },
  "bin": {
    "btr": "./dist/index.js"
  },
  "dependencies": {
    "@internal/fast-btr-build-js": "workspace:*",
    "commander": "^12.0.0",
    "esbuild-plugin-copy": "^2.1.1"
  }
}
