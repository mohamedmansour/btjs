{
  "name": "@btjs/parser-rust",
  "description": "Rust parser for the BTJS project",
  "version": "1.0.1",
  "files": ["dist"],
  "scripts": {
    "build": "cargo build --release",
    "test": "cargo test"
  }
}
