{
  "name": "@btjs/eval-js",
  "version": "1.0.1",
  "description": "Bindings for evalulating expressions",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "type": "module",
  "files": ["dist"],
  "scripts": {
    "build": "esbuild ./src/index.ts --bundle --outdir=./dist --sourcemap --format=esm --target=esnext",
    "test": "mocha -r esbuild-register '**/*.spec.ts'"
  }
}
