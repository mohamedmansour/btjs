{
  "name": "@btjs/server-rust",
  "description": "Rust server for the BTJS project",
  "version": "1.0.0",
  "files": ["dist"],
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "test": "cargo test"
  }
}
